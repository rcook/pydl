name: Changelog

# Security: triggered only by tag pushes (tag write is restricted to repo
# collaborators). `TAG_NAME` is sourced from `github.ref_name`, which is the
# matched tag (validated by the `v*.*.*` trigger pattern). It is passed via the
# step `env:` block and used inside double-quoted strings only — not splatted
# into a shell command.

on:
  push:
    tags:
      - v*.*.*

permissions:
  contents: write

jobs:
  update-changelog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main

      - name: Generate CHANGELOG.md
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --verbose
        env:
          OUTPUT: CHANGELOG.md

      - name: Commit CHANGELOG.md
        env:
          TAG_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if git diff --quiet CHANGELOG.md; then
            echo "CHANGELOG.md unchanged; nothing to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git commit -m "Update CHANGELOG.md for ${TAG_NAME}"
          git push origin main
